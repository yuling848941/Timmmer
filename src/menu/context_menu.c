/*
 * context_menu.c — 已废弃（死代码占位文件）
 *
 * 本文件原本是 LVGL（嵌入式图形库）的示例代码，
 * 内容包含 C++ lambda（auto add_item = [&]...）且依赖 lvgl/lvgl.h，
 * 无法被本项目（纯 C + Win32）编译，也未在 build.bat 中引用。
 *
 * 当前项目的右键/托盘菜单由 src/menu/ios_menu.c（Win32 原生实现）提供。
 * 本文件保留仅为占位，避免误将旧文件加入构建导致编译失败。
 * 如需删除本文件，可直接移除。
 */
