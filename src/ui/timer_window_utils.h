#ifndef TIMER_WINDOW_UTILS_H
#define TIMER_WINDOW_UTILS_H

#include <windows.h>

void SetWindowRoundedCorners(HWND hwnd, int cornerRadius);
void SetWindowShadow(HWND hwnd, BOOL enable);

#endif // TIMER_WINDOW_UTILS_H
